# Core database and API dependencies
sqlalchemy==2.0.44
# psycopg v3 is the only Postgres driver: core/db.py coerces every URL to
# postgresql+psycopg://. [binary] bundles libpq into the wheel so no system
# libpq/libpq-dev is needed in the container. (psycopg2-binary removed
# 2026-07-08 — zero importers, audit finding #5.)
psycopg[binary]==3.2.1

# FastAPI and web server
# fastapi 0.139.2 + starlette 1.3.1 close the Starlette advisory set
# (F10-SEC-01 B2.A.4). FastAPI no longer caps starlette, so the exact
# starlette version is pinned explicitly to keep resolution deterministic.
fastapi==0.139.2
starlette==1.3.1
uvicorn==0.27.1
slowapi==0.1.9  # Rate limiting

# LangChain ecosystem - compatible versions
# Only the two provider wrappers are imported (core/llm.py,
# core/llm_runtime.py, knowledge/vector_embeddings.py); langchain-core is
# their shared transitive runtime, pinned explicitly to keep resolution
# deterministic. Advisory-fixed set as of F10-SEC-01 B2.A.5 (2026-07-18):
# every constructor kwarg the code passes (request_timeout, max_tokens,
# base_url, convert_system_message_to_human, thinking_budget, dimensions,
# embed_documents(timeout=...)) was probe-verified against these exact
# versions. (langchain and langchain-community removed in B2.A.2 — zero
# importers.)
langchain-core==1.5.0
langchain-google-genai==4.3.1
langchain-openai==1.4.0
# langsmith is a transitive of langchain-core, but it patch-releases very
# frequently (0.10.6 -> 0.10.7 -> 0.10.8 within a day), and the hashed-lock
# gate re-resolves live, so each release reddens "Verify requirements lock is
# current" on every new CI run (hit twice during F10 B6). Pin it to the version
# certified with backend 65cf93b so the closure stays stable; bump deliberately
# alongside langchain upgrades.
langsmith==0.10.9

# Data validation and serialization
# 2.12.4 is the version the full local suite characterizes AND the version
# that generated the committed contracts/chat_gateway_v1.schema.json —
# pydantic 2.9.x emits a different JSON schema, which failed the CI contract
# drift gate the first time the full-test step executed (B2 follow-up,
# 2026-07-19). Compatible: fastapi 0.139.2 (>=2.9), langchain-core 1.4.9,
# google-genai (<3).
pydantic==2.12.4

# Data processing and analysis
# (statsmodels removed 2026-07-18 — zero importers, F10-SEC-01 B2.A.2.)
pandas==2.1.4
numpy==1.26.4
scipy==1.11.4  # For trendline calculations (linear regression)

# Authentication
# 2.13.0 closes the 2026 header/algorithm-hardening advisory set
# (F10-SEC-01 B2.A.3); the rejection policy is pinned by
# tests/test_auth_negative.py.
PyJWT==2.13.0

# Utilities
# tenacity has no direct importer (F10-SEC-01 B2.A.2); the exact pin is a
# transitive version hold for google-genai (<9.2.0). 9.1.4 is what the
# characterized local suite runs after the B2.A.5 wrapper upgrade.
tenacity==9.1.4
python-dotenv==1.2.2  # 1.2.2 closes CVE-2026-28684 (F10-SEC-01 B2.A.3)

# AI/ML services
# (litellm removed 2026-07-18 — zero importers, F10-SEC-01 B2.A.2;
# provider access goes through langchain-openai/langchain-google-genai.)
# (google-generativeai removed 2026-07-18 — F10-SEC-01 B2.A.5: the legacy
# SDK was only required by langchain-google-genai 0.0.x; its removal drops
# the entire protobuf/gRPC chain from the closure. The guarded legacy
# fallback in knowledge/vector_embeddings.py remains inert resilience code.)
google-genai==2.13.0

# SQL parsing
sqlglot==25.25.0
