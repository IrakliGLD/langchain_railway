# Development / CI-only tools (not installed in the Docker image)
ruff==0.15.16
# pytest-cov 5.x is the newest line compatible with the pytest==8.0.0 pin in requirements.txt
pytest-cov==5.0.0
# Production dependency advisory evidence (P7.5).
pip-audit==2.10.1
# CycloneDX SBOM generator used by the protected release-evidence workflow.
cyclonedx-bom==7.3.0
